requests:
  monkeyness:
    host: pirate.monkeyness.com
    requests:
      insult:
        path: /api/insult
      translate:
        path: /api/translate
        queryparams:
          english: where is the nearest restroom?

