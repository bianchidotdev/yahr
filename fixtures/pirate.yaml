requests:
  monkeyness:
    host: pirate.monkeyness.com
    headers:
      who-are-ye: {{ .ENV_MATEY }}
    requests:
      insult:
        path: /api/insult
      translate:
        path: /api/translate
        queryparams:
          english: where is the nearest restroom?
