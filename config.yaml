requests:
  google_get:
    host: google.com
    scheme: https
    path: /
  private_server_get:
    host: localhost:2222
    scheme: http
    path: /opl/health
    headers:
      Authorization: Bearer {{ .PRIVATE_SERVER_ENV_VAR }}_
